#include <gtest/gtest.h>

#include <QApplication>

#include "LoadImage.hpp"

TEST(LoadImageTest, startLoadingImagesFromFolder) {
    // std::shared_ptr<Data> data;
    // int argc = 1;
    // char* argv[1] = {(char*)"test"};
    // QApplication app(argc, argv);
    // QProgressDialog progress("Loading images...", "Abort", 0, 100);
    // startLoadingImagesFromFolder(nullptr, data, TESTS_PATH.toStdString() + "ressources/images", progress);
}